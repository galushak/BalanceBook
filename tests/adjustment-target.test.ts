import test from 'node:test';import assert from 'node:assert/strict';import {adjustmentInput} from '../src/forms';import {blankState} from '../shared/domain';
test('target balance computes signed historical adjustment and excludes edited entry',()=>{const s:any=blankState();s.accounts=[{id:'a',opening:95000,trackingStart:'2020-01-01'}];s.events=[{id:'later',date:'2026-09-10',status:'ACTIVE',entries:[{accountId:'a',amount:10000}]}];const d={type:'ADJUSTMENT',accountId:'a',date:'2026-09-01',actualBalance:'925.00'};assert.equal(adjustmentInput(s,d).amount,2500);assert.equal(adjustmentInput(s,d).direction,-1);assert.equal(adjustmentInput(s,{...d,actualBalance:'975'}).direction,1);assert.throws(()=>adjustmentInput(s,{...d,actualBalance:''}));assert.throws(()=>adjustmentInput(s,{...d,actualBalance:'950'}));const e={date:d.date,status:'ACTIVE',entries:[{accountId:'a',amount:-2500}]};s.events.push(e);assert.equal(adjustmentInput(s,d,e).amount,2500);assert.equal(adjustmentInput(s,{...d,actualBalance:'-100'}).amount,102500);});
